A user with **Partner Operations Agent** credentials:
1. Signs-in to the Sage Provisioning Portal  
2. Opens the **Customer** specific **CustomerDetails** page  
3. Opens the **Environment** tab
4. Clicks the **Add site** button and follows that process  
```mermaid
%%{
  init: {
    'theme': 'forest',
    'themeVariables': {
      'fontSize':'14pt'
    }
  }
}%%
flowchart LR
  classDef Sage fill:seagreen, color:honeydew;
  classDef Noun fill:lightcyan, stroke:royalblue, color:royalblue;
  classDef Note fill:white, stroke:white, text-align:left;

  PARTNER_ORGANISATION_01[Partner Organisation]

  PARTNER_ORGANISATION_01-.has a.->
  PARTNER_OPERATIONS_AGENT_01([&nbsp;Partner Operations Agent&nbsp;])

  PARTNER_OPERATIONS_AGENT_01-.adds<br/>**Site**.-
  SITE_01["Clouds - Sage 50<br/>(Site)"]

  PARTNER_ORGANISATION_01-.has a.->
  PRODUCT_01[Product]-.associated.-
  SITE_01

  PARTNER_ORGANISATION_01-.has a.->
  CUSTOMER_ORGANISATION_01[Customer Organisation]-.default<br/>association.-
  SITE_01

  CUSTOMER_ORGANISATION_01-.has a<br/>(required).->
  PRIMARY_CONTACT_01([&nbsp;Primary Contact&nbsp;])

  PRIMARY_CONTACT_01-.associated<br/>(required).-
  SITE_01

  class PARTNER_ORGANISATION_01,CUSTOMER_ORGANISATION_01,PRODUCT_01,SITE_01 Noun
```
<MessageCard type='proactive' label='Note'>A **Partner Operations Agent** will add a **Site** to a **Customer** organisation, so that association is made by default<br/><br/>During the process of adding a **Site** it is associated with a **Product**, and a default administrative user</MessageCard>
